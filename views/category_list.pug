extends layout

block content
  h1= title

  ul
    each category in category_list
      li
        a(href=category.url) #{category.name}
        p Number Of Equipment: #{category.equipment.length}

    else
      li There are no categories.
